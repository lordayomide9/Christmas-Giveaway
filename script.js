let shareCount = 0;

    function shareToGroups() {
      shareCount++;
      document.getElementById("shareCounter").innerText = `Shares: ${shareCount}`;
      document.getElementById("submitButton").disabled = false;
    }
    
   function shareToGroups() {
      shareCount++;
      document.getElementById("shareCounter").innerText = `Shares: ${shareCount}`;
      document.getElementById("submitButton").disabled = false;
    }

    function submitForm(event) {
      event.preventDefault(); // Prevents the default form submission
      alert("Congratulations! You are on the whitelist. You will be credited before 20th of December. 🎊 Share to more People");
      // You can add additional logic here for submitting data to a server if needed.
    }
    
        let shareCount = 0;

    function shareToGroups() {
      shareCount++;
      document.getElementById("shareCounter").innerText = `Shares: ${shareCount}`;
      document.getElementById("submitButton").disabled = false;
    }

    function submitForm(event) {
      event.preventDefault(); // Prevents the default form submission
      alert("Congratulations! You are on the whitelist. You will be credited before 20th of December. 🎊");
      // You can add additional logic here for submitting data to a server if needed.
    }